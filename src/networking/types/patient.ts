export type RawPatient = {
  createdAt: Date;
  name: string;
  avatar: string;
  description: string;
  website: string;
  id: string;
};

export type Patient = {
  name: string;
  avatar: string;
  description: string;
  website: string;
  id: string;
};
